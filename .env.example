# Copy to .env and fill in. Never commit the real .env.
# Blofin API credentials are only needed from Phase 1 onward.
# Generate keys with trade and read permissions, withdrawal permission OFF.
BLOFIN_API_KEY=
BLOFIN_API_SECRET=
BLOFIN_API_PASSPHRASE=
